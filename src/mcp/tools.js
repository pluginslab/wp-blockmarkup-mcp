/**
 * MCP tool definitions and handlers
 *
 * Tools:
 *   search_blocks         — FTS5 search across blocks (name, title, description, category)
 *   get_block_schema      — Full schema for a block (attributes, supports, variations)
 *   get_block_markup      — Validated markup examples, optionally filtered by features
 *   validate_markup       — Validate raw markup: structural + save function
 *   list_block_attributes — All attributes for a block with types/defaults/constraints
 *   search_variations     — Search variations by name/description
 */

// TODO: Implement MCP tool handlers
