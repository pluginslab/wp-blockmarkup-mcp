/**
 * Markup generator — creates block markup examples for validation and storage
 *
 * Generates examples for each feature combination:
 *   - Basic (no attributes)
 *   - With alignment
 *   - With colors (background, text, gradients)
 *   - With typography (font size, line height, font family)
 *   - With spacing (padding, margin)
 *   - With border (color, radius, style, width)
 *   - Full-featured (all supported features combined)
 *   - Per-variation (pre-configured attribute sets)
 *
 * Based on: gutenberg-block-extractor/src/utils/markup-generator.js
 */

// TODO: Port from gutenberg-block-extractor
