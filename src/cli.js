#!/usr/bin/env node
/**
 * CLI entry point for wp-blockmarkup-mcp
 *
 * Commands:
 *   source:add       — Register and index a new block source
 *   source:list      — List all registered sources
 *   source:remove    — Remove a source and its data
 *   index            — Re-index all or specific sources
 *   search <query>   — Full-text search across blocks
 *   validate <markup>— Validate block markup against save()
 *   stats            — Show block counts and validation coverage
 *   rebuild-index    — Rebuild FTS5 search indexes
 */

// TODO: Implement CLI with commander
